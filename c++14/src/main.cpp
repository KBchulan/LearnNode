//
// Created by whx on 24-10-21.
//

#include "../include/c++14.hpp"

int main(int argc, char **argv) {
    std::ios::sync_with_stdio(NULL);
    std::cin.tie(nullptr);
    std::cout.tie(nullptr);
    std::cout << " ------------------" << std::endl;
    {
        using namespace WIDELAMBDA;
        workspace_07();
    }
}
